{
  "name": "false-alarm",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.16.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0",
    "socket.io": "^2.5.0",
    "socket.io-client": "^2.5.0"
  },
  "scripts": {
    "start": "node src/server/server.js",
    "devfrontend": "react-scripts start",
    "devbackend": "nodemon src/server/server.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
